const numeros = [1, 3, 6, 9, 12, 15, 18, 21, 24, 27, 30]
let soma = 0
for (let i = 0; i < numeros.length; i++) {
  let n = numeros[i]
  soma += n
}
alert(`A soma de todos os numeros é ${soma}`)



if(typeof n === "number") {
  soma += n
}